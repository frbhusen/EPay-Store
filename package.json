{
  "name": "epay-store",
  "version": "1.0.0",
  "scripts": {
    "start": "cd backend && npm start",
    "build": "react-scripts build",
    "install": "cd backend && npm install"
  },
  "dependencies": {
    "axios": "^1.13.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
